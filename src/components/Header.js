/*The header component simply returns the app title and a slogan. */

const Header = () => {
  return (
    <header>

      <h1>Just Hangman</h1>
      <p>Can you guess the word?</p>
</header>
  )
};

export default Header;
